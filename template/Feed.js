import React from 'react';
import { View, Text } from 'react-native';

const Feed = ({ navigation }) => {
    return (
        <View>
            <Text>Feed</Text>
        </View>
    );
}

export default Feed;